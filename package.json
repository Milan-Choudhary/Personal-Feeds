{
  "name": "ai-content-platform",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "ai-engine",
    "shared"
  ],
  "scripts": {
    "dev": "echo \"Run workspace-specific scripts in each package.\"",
    "lint": "echo \"Add lint scripts per package.\""
  },
  "dependencies": {
    "concurrently": "^10.0.3"
  }
}
